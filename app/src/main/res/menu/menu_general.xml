<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_re_action"
        android:title="@string/txt_real_estate"
        android:icon="@drawable/ic_type_white"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/nav_re_add"
                android:icon="@drawable/ic_add_blue"
                android:title="@string/default_txt_add" />
            <item
                android:id="@+id/nav_re_detail"
                android:title="@string/default_txt_search"
                android:icon="@drawable/ic_search_blue">
            </item>
        </menu>
    </item>

</menu>